package day16collection.Set.treeSet_comparator;

import java.util.Comparator;

/**
 * 定制排序    Comparator 的实现类
 *  命名 类型名Comparator
 *
 * */
public class EmpMycomparator implements Comparator {
    @Override
    public int compare(Object o1, Object o2) {
        return 0;
    }
}
